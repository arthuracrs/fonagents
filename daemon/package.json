{
  "name": "@fonagents/daemon",
  "version": "0.0.0",
  "description": "Daemon process that wires all adapters together and runs the orchestrator.",
  "main": "./dist/cli.js",
  "bin": {
    "fonagents": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc && esbuild src/cli.ts --bundle --platform=node --outfile=dist/cli.js --format=cjs",
    "typecheck": "tsc --noEmit",
    "dev": "tsx src/cli.ts",
    "start": "node dist/cli.js"
  },
  "dependencies": {
    "@fonagents/core": "^0.0.0",
    "@fonagents/beads-adapter": "^0.0.0",
    "@fonagents/anagent-adapter": "^0.0.0",
    "@fonagents/http-sse-adapter": "^0.0.0",
    "express": "^5.2.1"
  },
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^22.0.0",
    "esbuild": "^0.25.0",
    "tsx": "^4.21.0",
    "typescript": "^5.5.0"
  }
}
