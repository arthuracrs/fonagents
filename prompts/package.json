{
  "name": "@fonagents/prompts",
  "version": "0.0.0",
  "description": "All prompts used across the fonagents application — manager, worker, and UI.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit"
  },
  "license": "ISC",
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}
