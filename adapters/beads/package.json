{
  "name": "@fonagents/beads-adapter",
  "version": "0.0.0",
  "description": "Beads (bd CLI) adapter implementing IssueTrackerPort for @fonagents/core.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "dev": "tsc --watch"
  },
  "dependencies": {
    "@fonagents/core": "^0.0.0"
  },
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0"
  }
}
